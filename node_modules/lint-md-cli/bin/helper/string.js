"use strict";

exports.rightPad = function (s, len) {
  var c = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : ' ';
  var i = -1;
  var length = len - s.length;
  var str = s;

  while (++i < length) {
    str += c;
  }

  return str;
};