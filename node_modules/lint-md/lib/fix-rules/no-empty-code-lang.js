"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;

var _Text = _interopRequireDefault(require("../helper/Text"));

var _lodash = _interopRequireDefault(require("lodash"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

var _default = function _default(markdown, error) {
  var ast = error.ast;
  var position = ast.node.position;
  var start = position.start,
      end = position.end;
  var line = start.line;
  var text = new _Text["default"](markdown); // 获得当前行

  var lineText = text.getLine(line);
  var replaceText = "".concat(_lodash["default"].trimEnd(lineText), "plain"); // 将当前行替换成带语言的文本

  return new _Text["default"](markdown).removeLine(line) // 删除
  .insertLines(line - 1, replaceText).result(); // 插入
};

exports["default"] = _default;