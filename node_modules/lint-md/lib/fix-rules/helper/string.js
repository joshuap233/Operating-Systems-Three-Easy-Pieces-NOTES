"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.insertSpace = void 0;

var _lodash = _interopRequireDefault(require("lodash"));

var _string = require("../../lint-rules/helper/string");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

var matches = ['ZA', 'AZ', // 中文和英文
'ZN', 'NZ'];

var splice = function splice(s, start, deleteCount) {
  var arr = s.split('');

  for (var _len = arguments.length, strings = new Array(_len > 3 ? _len - 3 : 0), _key = 3; _key < _len; _key++) {
    strings[_key - 3] = arguments[_key];
  }

  arr.splice.apply(arr, [start, deleteCount].concat(strings));
  return arr.join('');
};

var insertSpace = function insertSpace(string) {
  var tmp = string;

  for (var i = 0; i < tmp.length; i++) {
    var s = (0, _string.stringType)(tmp.substr(i, 2)); // 匹配到

    if (_lodash["default"].includes(matches, s)) {
      tmp = splice(tmp, i + 1, 0, ' ');
    }
  }

  return tmp;
};

exports.insertSpace = insertSpace;